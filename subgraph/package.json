{
  "name": "identities",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ sudhamjayanthi/identities",
    "create-local": "graph create --node http://localhost:8020/ sudhamjayanthi/identities",
    "remove-local": "graph remove --node http://localhost:8020/ sudhamjayanthi/identities",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sudhamjayanthi/identities"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.2",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
